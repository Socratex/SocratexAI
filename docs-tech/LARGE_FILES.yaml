index:
- quick_index
- summary
- large_file_index
- maintenance
items:
  quick_index:
    title: Quick Index
    content: |
      - 📌 Summary
      - 📊 Large File Index
      - 🛠 Maintenance
  summary:
    title: 📌 Summary
    content: |
      📌 Generated index of code files above the large-file threshold.

      📌 This document is generated from `docs-tech/CODE_LINE_INDEX.json` by `Tools/update_code_line_index.ps1`. It tracks code files above 300 non-empty lines so large files have an explicit reason to stay large or a visible reason to split later.
  large_file_index:
    title: 📊 Large File Index
    content: |
      📊 Current files above 300 non-empty code lines.

      | Lines | File | Why It Is Large |
      | ---: | --- | --- |
      | 0 | _(none)_ | No tracked code files exceed the threshold. |
  maintenance:
    title: 🛠 Maintenance
    content: |
      🛠 Update this document through the line-index script instead of editing the table manually.

      🛠 Use `Tools/update_code_line_index.ps1` for a full refresh. Use `Tools/update_code_line_index.ps1 -ChangedOnly` when only changed files need to update their index records from the current git diff.
meta:
  document:
    title: 📏 Large Files
    type: technical
    language: en
  routing:
    purpose: Generated YAML index of code files above the large-file threshold.
    read_when:
    - When checking large source-owned files or deciding whether a file should be split.
    - When code line index tooling is in scope.
    do_not_read_when:
    - When the current task is unrelated to repository size, ownership, or tooling.
