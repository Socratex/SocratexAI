{
    "index": [
        "quick_index",
        "summary",
        "large_file_index",
        "maintenance"
    ],
    "items": {
        "quick_index": {
            "title": "Quick Index",
            "content": "- 📌 Summary\n- 📊 Large File Index\n- 🛠 Maintenance"
        },
        "summary": {
            "title": "📌 Summary",
            "content": "📌 Generated index of code files above the large-file threshold.\n\n📌 This document is generated from `docs-tech/CODE_LINE_INDEX.json` by `tools/codebase/update_code_line_index.py`. It tracks code files above 300 non-empty lines so large files have an explicit reason to stay large or a visible reason to split later."
        },
        "large_file_index": {
            "title": "📊 Large File Index",
            "content": "📊 Current files above 300 non-empty code lines.\n\n| Lines | File | Why It Is Large |\n| ---: | --- | --- |\n| 1492 | `tools/knowledge/knowledge_index.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 554 | `tools/json/json_list_doc.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 420 | `tools/pipeline/pipeline_sweep.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 415 | `tools/pipeline/rebuild_ai_compiled_context.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 392 | `tools/documents/document_read_cache_engine.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 365 | `tools/knowledge/knowledge_tier_report.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 322 | `tools/pipeline/tool_handler.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 303 | `tools/json/json_wrapper_entrypoints.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |\n| 303 | `tools/pipeline/Initialize-SocratexPipeline.py` | Repository helper script above the size threshold. Keep it documented here because it performs non-trivial tooling logic. |"
        },
        "maintenance": {
            "title": "🛠 Maintenance",
            "content": "🛠 Update this document through the line-index script instead of editing the table manually.\n\n🛠 Use `python3 -B tools/codebase/update_code_line_index.py` for a full refresh. Use `python3 -B tools/codebase/update_code_line_index.py --changed-only` when only changed files need to update their index records from the current git diff."
        }
    },
    "meta": {
        "document": {
            "title": "📏 Large Files",
            "type": "technical",
            "language": "en"
        },
        "routing": {
            "purpose": "Generated JSON index of code files above the large-file threshold.",
            "read_when": [
                "When checking large source-owned files or deciding whether a file should be split.",
                "When code line index tooling is in scope."
            ],
            "do_not_read_when": [
                "When the current task is unrelated to repository size, ownership, or tooling."
            ]
        }
    }
}
