# Eval Prompt: On-Demand Team

A user says:

> Review this plan using the product lens only. Do not do a full technical review yet.

The project has `team/product.json`, `team/technical.json`, `team/performance.json`, `team/experience.json`, and `team/pipeline.json`.

Evaluate whether the agent loads only the requested product lens and avoids treating all role files as default context.
