# TODO

## Current Priority

- Investigate the artifact-based task-flow audit as the next source-pipeline pass, with the adversarial diff audit folded in as a conditional end-of-task section for complex/high-risk diffs. Scope: define a task-close check that cites artifacts already produced during work (plan, research/mechanism, human-in-loop answers, lightweight-futureproof check, verification), then runs an adversarial diff review only when the estimated risk is architecture-relevant, hot-path, performance-sensitive, broad, or future-foundation. Keep tiny obvious fixes lightweight. Update SocratexPipeline featurelist/feature contracts and compiled context; then sync the updated package into RiftboundVanguard and update RiftboundVanguard's instance featurelist/context before more Riftbound implementation work.

## Side Backlog

- Fragmented-task grouped auto-orchestration workflow: optimize SocratexPipeline for context fragmentation by adding an explicit rule that a task with more than roughly 1500 LOC of required code/context, more than 3 substantial substeps, or broad ownership/performance/migration scope must be split into groups and bounded sub-tasks. A group is one user-approved execution unit; sub-tasks inside that group run automatically without waiting for another user `continue`. Between sub-tasks, the agent must clear/minimize working context, reload only project context by tier plus the group sub-plan/handoff, execute the next bounded sub-task, and write an updated STATE/PLAN handoff. Fragment N must record what it inspected, what it changed, current hypothesis/decision state, unresolved risks, exact next files/commands, and verification status so fragment N+1 can continue from files instead of relying on lost chat context. The agent waits for user `continue` only before starting the next group, not between sub-tasks in the active group. Treat STATE/PLAN as external memory that simulates long context over time. Keep thresholds configurable/profile-aware and preserve a stop/escalation rule for blockers, unexpected broad scope, failing verification, or user-facing decisions.
- Full SocratexAI and child-project knowledgebase tier migration: run a real audit after the current pipeline pass chain, not a shallow sweep. First inventory source documents and report before counts for explicit `context_tier`, defaulted tier, and ambiguous entries. Add explicit `context_tier` to entries that belong to one Tier 1-5 layer. If one entry mixes multiple tiers, split it into multiple focused entries before assigning tiers. Keep files physically where they are for the first pass so future tooling can migrate by scanning JSON nodes for `context_tier`; only later consider file-per-tier separation through a migration tool. Report after counts, split entries, unresolved ambiguous entries, and recommended tooling such as `move_entries_by_context_tier`.
- Task-specific research formalization flow: improve the pipeline so broad philosophy is converted into concrete task plans through contextual research instead of by stuffing every possible implementation-specific rule into always-loaded directives. The target code-touch flow is always-on: if a research-backed implementation plan already exists for the touched scope, execute it; if it does not exist, observe the task shape, load compact philosophy and project context, research known approaches/archetypes for this task/profile (for example AAA-style traversal, business bulk data, state management, security, migration strategy), share concise findings and tradeoffs, ask targeted scale/load/architecture questions when future pressure is unclear, formalize the plan with research conclusions, then execute or leave the plan ready for the next pass. This should prevent framing-inheritance failures where the agent inherits the user's symptom framing, such as "FPS drops, optimize like AAA", instead of noticing that the generic philosophy requires researching the structural model first. Preserve generic directives like "borrowed before bespoke", "better version now", and "avoid future refactor debt" while avoiding core directives that enumerate every domain-specific tactic such as physics indexing, ORM batching, or state-management details. Add an overengineering check: if research-driven better-version work becomes large or speculative, ask before expanding scope. This is a possible future pipeline feature, not an immediate always-loaded rule.
- Artifact-based task-flow audit with conditional adversarial diff review after code-touch tasks: investigate a lightweight end-of-task verification of positive flow execution instead of enumerating negative symptoms. For code-touch commits, the agent should answer with artifacts, not yes/no self-grading: cite the plan section executed; cite the research conclusion or known-solution finding that chose the implementation direction; state the lightweight/futureproof check result, cite human-in-loop answers when required, explain why a check was not needed, and flag "no, but should have" into STATE/report when the flow was skipped incorrectly. Use a three-state answer per check: `yes + artifact`, `no, not needed + reason`, or `no, but should have`. Add a risk classifier based on artifacts already gathered during the task; if the diff is architecture-relevant, hot-path, performance-sensitive, broad, or future-foundation, run an extra adversarial section that reads the diff plus compact project profile and asks whether the change inherited an unstated framing assumption, missed a better known architecture, optimized symptoms instead of the structural model, or introduced future retrofit debt. Possible implementation: one thin always-loaded reference to invoke `task_flow_audit`, a prompt/template outside core context, a `tools/quality/task_flow_audit.ps1` script, and an advisory quality-gate entry for code changes only. This should make "wrong" mean "not according to the main flow" while keeping the heavier adversarial review conditional.
